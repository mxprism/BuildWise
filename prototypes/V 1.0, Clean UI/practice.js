function add(a, b) {
   return a + b;
}

function multiply (a, b) {
   return a * b;
}

function square (number) {
   return multiply(number, number);
}
 const person = {
   name: 'John',
   age: 30, 
   country: "USA"
 }

 const calculations = [];
 calculations.push ({
   material: 'wood',   
   quantity: 10,
   price: 5,
   total: 50
 })
